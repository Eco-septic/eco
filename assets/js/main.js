console.log("This site was generated by Hugo.");

// main.js
import { toggleTheme, setupThemeToggle } from "./theme.js";
import { setupIntersectionObservers } from "./animation.js";
import { heroPagination } from "./hero.js";
import { scrollPercent } from "./scrollPercent.js";
import { productPagination, newsPagination } from "./pagination.js";
window.toggleTheme = toggleTheme;
setupThemeToggle();

document.addEventListener("DOMContentLoaded", () => {
  const productTitle = document.getElementById("productTitle");
  const productDescription = document.getElementById("productDescription");
  const newsTitle = document.getElementById("newsTitle");
  const newsDescription = document.getElementById("newsDescription");
  const newsItems = document.querySelectorAll(".news-card");
  const filterButtons = document.querySelectorAll(".filter-button");
  const productItems = document.querySelectorAll(".product-item");
  // Set up intersection observers for animations
  setupIntersectionObservers(
    productTitle,
    productDescription,
    newsTitle,
    newsDescription,
    productItems,
    filterButtons,
    newsItems,
  );
});

document.addEventListener("DOMContentLoaded", productPagination);
document.addEventListener("DOMContentLoaded", newsPagination);

document.addEventListener("DOMContentLoaded", scrollPercent);
document.addEventListener("DOMContentLoaded", heroPagination);
